rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT PLACEHOLDER ONLY.
    // Replace with user/role-based rules before production.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
